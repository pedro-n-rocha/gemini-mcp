# Copy to .env and fill in your values.
# Never commit .env.

GOOGLE_OAUTH_CLIENT_ID="...apps.googleusercontent.com"
GOOGLE_OAUTH_CLIENT_SECRET="..."

# Optional: credentials directory (defaults to repo directory)
# CREDENTIALS_PATH="."

# Optional: HTTP bind address + port (MCP SSE server)
# HOST="0.0.0.0"
# HTTP_PORT="8080"  # preferred
# PORT="8080"       # legacy alias

# Optional: transport ("sse" or "stdio")
# MCP_TRANSPORT="sse"
